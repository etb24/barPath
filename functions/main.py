"""Firebase Functions codebase — currently empty.

Video baking (drawing the bar path into a shareable MP4) moved on-device; the app
now uses the phone's hardware encoder via @azzapp/react-native-skia-video, so the
former `bake_video` callable was retired. No functions are defined here.

"""
